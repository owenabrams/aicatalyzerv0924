from flask import Flask
from dotenv import load_dotenv
import os

def create_app():
    app = Flask(__name__)

    # Load environment variables from .env
    load_dotenv()

    # Apply Flask configuration from environment variables or config.py
    app.config.from_object('app.config.Config')

    # Register blueprints, initialize services, etc.
    return app

